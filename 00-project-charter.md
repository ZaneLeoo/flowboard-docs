# 00｜项目章程

## 1. 产品定义

**产品名：** FlowBoard  
**一句话：** 帮助个人把目标、项目和当天最重要的任务聚合到一个安静、有掌控感的工作台。  
**产品类型：** 面向个人使用者的 Web 应用（C 端效率工具），支持桌面端优先、移动端适配。

FlowBoard 不是传统的企业后台，也不是功能堆砌的 Jira 克隆。第一版的重点是让用户在极短时间内完成“今天做什么、项目进展如何、下一步是什么”的判断和操作。

## 2. 项目目标

1. 完成一个可公开部署、可持续迭代的前后端分离产品。
2. 练习从需求、设计、开发、测试、发布到回滚的完整交付流程。
3. 以前后端独立仓库和独立 Jenkins Pipeline，练习真实的 CI/CD 边界。
4. 建立一套不依赖 Element Plus 的 C 端设计系统与交互规范。

## 3. 成功标准

| 维度 | 可验证标准 |
| --- | --- |
| 产品 | 新用户能在 3 分钟内创建项目和第一项任务，并在“今日”视图完成或调整任务。 |
| 工程 | 任一仓库推送到 `main` 后，Jenkins 能自动测试、构建、部署对应服务。 |
| 可靠性 | 部署后健康检查失败时，不切换到新版本；可回滚到上一个镜像标签。 |
| 体验 | 核心路径在桌面和手机浏览器可用；键盘、焦点、低动态偏好不被破坏。 |
| 文档 | 所有已实现需求均能追溯到需求编号、接口、测试和验收记录。 |

## 4. MVP 边界

### MVP 包含

- 账号注册、登录、退出和刷新登录态。
- 个人工作空间、项目、任务的创建、编辑、归档与查询。
- 今日任务、项目列表、看板状态和任务详情。
- 任务优先级、截止日、标签、备注和完成状态。
- 基础统计：今日完成数、逾期数、项目完成度。

### MVP 明确不包含

- 多人实时协作、邀请、评论、通知中心。
- 原生 App、离线同步、AI 自动拆解任务。
- 支付、订阅、复杂权限、多租户计费。
- 微服务、Kubernetes、消息队列。

这些能力在 MVP 验证稳定后再以独立需求进入路线图。

## 5. 关键决策

| 编号 | 决策 | 原因 |
| --- | --- | --- |
| ADR-001 | 前端、后端使用独立仓库 | 可独立构建、发布与回滚，适合练习 Jenkins CI/CD。 |
| ADR-002 | 共享文档使用独立轻量仓库 | 避免需求、API 和部署说明依附任意一端。 |
| ADR-003 | MVP 先做单用户个人工作空间 | 聚焦核心体验，降低账号协作和权限模型复杂度。 |
| ADR-004 | 先做模块化单体后端 | 先掌握交付链路；拆服务必须有性能或组织边界依据。 |
| ADR-005 | 不使用 Element Plus | 保留自定义视觉与交互的控制权，避免后台化观感。 |
| ADR-006 | MVP 使用简体中文并预留国际化 | 先保证中文体验完整；前端从第一天采用国际化键值，英文在完成全量翻译后再开放。 |

## 6. 待确认项

- 云服务器的操作系统、域名、镜像仓库和是否预留 staging 环境。
- Git 远程托管使用 GitHub 还是 Gitee。
