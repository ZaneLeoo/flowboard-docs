# 04｜UI / UX 设计规范

## 1. 体验定位

FlowBoard 应传达 **平静、清晰、轻盈的掌控感**。它借鉴 Apple 对直接操控、空间一致性和细节反馈的原则，但不复制任何 Apple 的视觉资产或界面。

界面优先呈现“下一件值得做的事”，再展示项目结构和统计。避免后台管理系统常见的重边框、密集表格、永久展开的侧栏和大面积装饰色。

MVP 的产品界面与系统反馈统一使用简体中文。中文文案应短、直接、以动词表达操作，例如“新建任务”“延后到明天”“已完成”；不混用生硬的英文产品术语。

## 2. 视觉基础

| 元素 | 规则 |
| --- | --- |
| 字体 | 优先使用系统字体栈；大标题可收紧字距，正文保持舒适行高。 |
| 色彩 | 使用中性画布色、近黑文字、一个高辨识度主色与少量语义色；项目颜色只作定位，不承担正文对比度。 |
| 空间 | 以 4px 为最小节奏，主要间距使用 8、12、16、24、32、48px。 |
| 圆角 | 控制在少量层级：控件小圆角、卡片中圆角、抽屉大圆角；不让所有元素都像胶囊。 |
| 阴影与玻璃 | 仅用于浮层、工具栏和抽屉的层级表达；不在普通内容卡片上滥用模糊。 |

设计令牌由 `flowboard-web` 的 CSS 变量维护，页面不得随意写死颜色、阴影和动画曲线。

## 3. 响应式结构

| 断点 | 导航与布局 |
| --- | --- |
| 手机（< 768px） | 底部导航；任务详情使用可拖拽底部抽屉；主要操作位于拇指易达区。 |
| 平板（768–1199px） | 可折叠侧栏；项目和任务保持双层浏览。 |
| 桌面（≥ 1200px） | 左侧导航、中央内容、按需出现的右侧任务详情面板。 |

## 4. 组件策略

自定义组件：`AppButton`、`AppInput`、`TaskCard`、`ProjectCard`、`EmptyState`、`ProgressRing`、`TaskSheet`。  
基础行为组件：仅从 Radix Vue 使用 Dialog、Popover、Tooltip、Menu、Select 等无样式原语。

每个可点击组件都必须具备默认、悬停（仅精确指针）、按下、禁用、加载、键盘焦点和错误状态。

## 5. 交互动效

1. 按下立即反馈；按钮可使用约 `scale(0.97)` 的短暂缩放。
2. 高频操作（键盘切换、密集列表）不使用延迟动画。
3. 普通浮层使用短促、以 `transform` 和 `opacity` 为主的过渡；从触发源的方向和原点出现。
4. 拖拽任务、移动端抽屉等手势交互使用可打断的弹簧运动：手指移动时 1:1 跟随，释放时保留速度并对边界做阻尼。
5. 默认不做无意义循环动效；成功完成任务可提供一次克制的状态反馈。

## 6. 可访问性与包容性

- 键盘可到达所有交互控件；可见焦点不可移除。
- 使用语义 HTML、标签、错误关联与合适的 ARIA 属性。
- `prefers-reduced-motion: reduce` 下将位移/弹簧改为短淡入淡出。
- `prefers-reduced-transparency: reduce` 下增大浮层背景不透明度、移除模糊。
- 颜色不作为唯一状态提示；错误、完成和优先级均有文本或图标辅助。
