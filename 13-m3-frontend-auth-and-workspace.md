# 13｜M3：前端认证与工作区界面设计

## 1. 阶段目标

M3 先交付一个可真实登录的中文 C 端纵向切片：用户能注册、登录、恢复会话、退出，并进入符合视觉方向稿的 Today 工作区框架。

本阶段只接入已存在的 M2 认证接口。项目和任务 API 尚未实现，因此 Today 中的任务和项目区域必须展示有引导意义的空状态，不得用本地假数据伪装成已保存的用户数据。

## 2. 路由与会话

| 路径 | 作用 | 行为 |
| --- | --- | --- |
| `/login` | 登录 | 调用 `POST /auth/login`，成功后进入 `/today`。 |
| `/register` | 注册 | 调用 `POST /auth/register`，成功后直接进入 `/today`。 |
| `/today` | 工作区首页 | 守卫等待 refresh 会话恢复；无会话则跳转登录。 |
| `/` | 根路径 | 根据会话跳转 `/today` 或 `/login`。 |

- access token 只放在 Pinia 的运行内存中。
- 页面刷新时，前端调用 `POST /auth/refresh`，浏览器自动携带 HttpOnly refresh Cookie；成功后调用 `GET /me`。
- 所有请求都包含 `credentials: 'include'`；收到一次 `401` 时统一尝试刷新并重放原请求，刷新失败则清除会话并回到登录页。
- 不向 `localStorage`、`sessionStorage`、URL 或日志写入任何令牌。

## 3. 界面信息架构

### 3.1 桌面端（≥ 1024px）

```text
┌──────── 侧栏 ────────┬──────────── Today 主内容 ────────────┐
│ FlowBoard            │ 今天 / 日期 / 当前用户              │
│ 今天                 │                                    │
│ 项目（未开放）       │ 焦点任务区：空状态与唯一下一步       │
│                       │                                    │
│ 用户菜单 / 退出      │ 项目概览区：等待项目 API 接入        │
└─────────────────────┴────────────────────────────────────┘
```

方向稿中的暖白画布、深色排版、蓝色主操作、克制阴影和轻量侧栏会保留。认证页采用冷灰外画布与白色圆角主容器：左侧为纯 CSS 绘制的抽象拼贴图形，右侧为最大约 400px 宽的轻量表单。主容器仅承担页面结构，不把表单放大成巨型卡片；不使用深色半屏宣传区、图片资源或 Google 等第三方快捷登录。未实现的任务与项目能力不显示伪造数据，也不展示不可用的高优先级主按钮。

### 3.2 移动端（< 1024px）

- 侧栏收起为顶部品牌和账户菜单；内容单列。
- 焦点区和项目概览使用纵向卡片，触控目标不小于 44px。
- 后续任务详情改用底部抽屉；M3 当前不创建空的抽屉交互。

## 4. 组件与状态

| 层 | 组件 / 文件 | 职责 |
| --- | --- | --- |
| `lib` | `http.ts` | 请求、401 刷新、错误解析与重放；不依赖视图。 |
| `stores` | `auth.ts` | access token、当前用户、会话恢复、登录注册退出。 |
| `components/ui` | Button、TextField、Toast | 一致的表单、即时按下反馈、错误与完成提示。 |
| `components/layout` | AppShell、SideNav | 响应式工作区框架与路径提示。 |
| `views` | Login、Register、Today | 路由页面组合，不直接发起 `fetch`。 |

## 5. 视觉与交互准则

本阶段依据 `apple-design` 规范实施：

- 按钮在 pointer-down 立即给出轻微缩放反馈；只动画 `transform` 与 `opacity`。
- 非手势面板使用短暂、无反弹的进入与退出过渡；同一元素沿相同路径返回。
- 侧栏与用户菜单使用少量半透明材质，正文卡片保持实底，避免多层玻璃叠加。
- 标题采用紧凑字距和较短行高；中文正文保留舒适行距，确保窄屏可读。
- 支持 `prefers-reduced-motion`、`prefers-reduced-transparency` 和更高对比度偏好。

## 6. 验收标准

1. `npm run build` 通过，TypeScript 无错误。
2. 注册、登录、刷新会话、退出均只通过 M2 API 工作，不持久化 access token。
3. 未登录访问 `/today` 会跳转登录；已登录访问认证页会跳转 Today。
4. Today 的桌面与移动布局符合方向稿的层级和中文规范，且项目/任务缺失时展示清晰空状态。
5. 网络、表单和认证失败均有中文反馈，键盘可完成表单操作。
