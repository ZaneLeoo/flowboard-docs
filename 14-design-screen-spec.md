# 14｜设计系统与页面规范 v1

## 1. 目的与范围

本文将已确认的 v2 视觉方向转为可实现、可验收的界面约束。覆盖 MVP 的认证、首次工作区、Today、项目和任务创建/详情；不新增 PRD 外的业务能力。

视觉基线见 [09-visual-direction.md](09-visual-direction.md)。当前状态为**已确认，待实现**。

## 2. 基础令牌

| 类别 | 令牌 | 值 / 规则 |
| --- | --- | --- |
| 画布 | `--surface-canvas` | 暖灰白 `#F7F6F3`。 |
| 面板 | `--surface-raised` | 白色 `#FFFFFF`；正文卡片保持实底。 |
| 主文字 | `--text-primary` | 近黑 `#1C1D21`。 |
| 次级文字 | `--text-secondary` | 低对比中性灰，不能承担唯一状态提示。 |
| 主色 | `--accent-primary` | 蓝色 `#2463EB`；hover 使用更深蓝，按下使用即时轻微缩放。 |
| 留白 | `--space-*` | 4 的倍数：8、12、16、24、32、48px。 |
| 圆角 | `--radius-*` | 控件 8px、卡片 12–16px、抽屉/大容器 24–32px。 |
| 阴影 | `--shadow-*` | 仅浮层、抽屉、悬浮工具栏可用；普通列表不加重阴影。 |

字体使用系统中文字体栈。页面标题紧凑字距、较短行高；正文保持正常字距和舒适行高。不得在业务组件中写死色值、阴影与动画曲线。

## 3. 品牌标识规范

| 项目 | 规范 |
| --- | --- |
| 形态 | 两个蓝色色块组合的几何 F 标识，旁配 `FlowBoard` 字标。 |
| 源文件 | 以本地 SVG 实现；提供图标版和横向字标版。 |
| 使用 | 侧栏、认证/引导页用横向字标；窄侧栏与应用图标用图标版。 |
| 颜色 | 默认深浅蓝双色；单色场景使用 `--text-primary` 或白色，不使用渐变替代。 |
| 留白 | 标识四周至少保留图标宽度 25% 的安全距离。 |

## 4. 页面规范

### 4.1 登录与注册

- 外层：浅冷灰/暖灰背景；主容器为大圆角白色结构面板，不做黑白分屏。
- 桌面：左侧约 45% 的抽象几何拼贴，右侧为最大 400px 的表单；表单本身不再套大型卡片。
- 移动：隐藏大面积拼贴，只保留小型品牌标识；表单占满安全边距内的宽度。
- 文案：登录标题“欢迎回来”，注册标题“创建账号”；不显示“登录 FlowBoard”与“返回首页”。
- 操作：仅邮箱、密码与主按钮；不出现第三方快捷登录。网络/校验错误显示在字段附近或表单顶部。

### 4.2 首次工作区

- 首屏唯一必填项为“工作区名称”；标题为“创建你的工作区”。
- 使用一幅小型、非阻塞的几何插画表达完成感；不要堆叠欢迎语、说明和多个步骤。
- 提交成功后进入 Today；项目创建为可选的下一步，不拦截进入工作区。

### 4.3 Today：桌面端（≥ 1200px）

```text
┌──── 左侧导航 ────┬──────────── 中央任务流 ────────────┬── 任务详情（按需） ──┐
│ 品牌 / 今日 / 项目 │ 日期、焦点、创建任务               │ 标题、状态、项目、截止日 │
│ 用户菜单           │ Focus / Plan / Later / Done 分组   │ 描述、标签、操作          │
│                    │ 项目进度摘要                        │                           │
└───────────────────┴─────────────────────────────────┴────────────────────────┘
```

- 侧栏是稳定导航，不重复呈现页面标题；当前项用蓝色细条与文字强调。
- 中央区优先显示任务，不用大面积 Hero 占据首屏。任务行以复选控件、标题、必要的项目/截止信息组成。
- 右侧详情只在选择任务后出现；关闭后中央内容自然回收，不保留空白大栏。
- “新建任务”为唯一高优先级页面操作，使用蓝色按钮；项目色、进度条和图标仅作为辅助信息。

### 4.4 Today：移动端（< 768px）

- 顶部显示“今天”和日期；正文为一列任务分组；底部固定导航提供“今天”“项目”“我的”等少量目的地。
- 新建任务可用底部悬浮蓝色按钮唤起；触控目标不小于 44px。
- 点击任务后以 `TaskSheet` 从底部展开。抽屉有拖拽把手、明确关闭路径、半透明遮罩和安全区底部留白。
- 抽屉不可把桌面字段全部平铺；先显示标题、状态、项目、截止日与核心操作，次要设置折叠或进入二级页。

### 4.5 项目总览与详情

- 项目总览以项目名称、色点、未完成数和进度为核心；没有项目时提供“新建项目”这一条清晰路径。
- 项目详情默认列表视图；看板和拖拽排序属于渐进增强，不能阻断任务的创建、完成和编辑。
- 归档后给出带“撤销”的短时 Toast。只有不可恢复的永久删除才使用确认弹层。

### 4.6 新建任务与新建项目

- 桌面端用居中 Dialog；移动端用底部抽屉。两者字段与校验规则一致。
- 新建任务优先字段：任务名称、项目（可选）、状态、截止日；描述、标签、优先级归为次级设置。
- 新建项目优先字段：名称、颜色；初次创建不强制填写描述和复杂配置。
- 提交中按钮显示加载状态并保持布局；成功后关闭容器、更新列表并给出简短反馈；失败保留输入内容。

## 5. 组件状态与交互

| 组件 | 必备状态 | 交互约束 |
| --- | --- | --- |
| `AppButton` | 默认、hover、按下、禁用、加载 | 指针按下立即缩放约 `0.97`；键盘焦点清晰可见。 |
| `AppInput` | 默认、聚焦、已填、错误、禁用 | 标签、帮助和错误提示关联到输入控件。 |
| `TaskRow` | 默认、hover、选中、完成、拖拽中 | 复选操作不必先打开详情；完成后的撤销路径明确。 |
| `TaskSheet` | 关闭、打开、拖拽中、提交中 | 1:1 跟手、阻尼边界、按释放速度决定归位或关闭。 |
| `AppDialog` | 关闭、打开、提交中、错误 | 焦点锁定、Esc 关闭、关闭后焦点回到触发按钮。 |
| `AppToast` | 成功、警告、错误、撤销 | 不阻塞阅读与后续操作；重要撤销动作有足够显示时间。 |

动效只使用 `transform` 和 `opacity`。普通浮层使用短促无反弹过渡；抽屉和拖拽可使用可打断的弹簧。`prefers-reduced-motion` 下改为短淡入淡出，`prefers-reduced-transparency` 下提高面板不透明度并取消模糊。

## 6. 实施顺序与验收

1. 先落地令牌、SVG Logo、`AppButton`、`AppInput`、Dialog/Sheet 和 Toast 原语。
2. 实现认证与首次工作区，确认表单比例、品牌标识和中文文案。
3. 以桌面 Today 为主页面实现任务流和按需详情；再实现移动底部导航和 `TaskSheet`。
4. 接入项目总览、项目详情、新建任务/项目与归档撤销。

每个页面完成时至少检查：桌面与手机布局、键盘路径、加载/空/错误状态、中文截断、主色使用、减少动效偏好，以及与本规范的字段和层级一致性。
