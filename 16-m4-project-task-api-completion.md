# 16｜M4.1：项目、任务与 Today 后端验收记录

## 1. 结论

M4.1 后端已完成并推送至 `flowboard-api` 的 `main` 分支，提交为 `191ba6f 功能：实现项目任务与 Today 核心接口`。

本次交付的是可持久化、按工作空间隔离的项目与任务能力，不是前端演示数据。当前前端 `TodayView` 仍是 M3 阶段的登录后骨架，M4.2 将基于本接口重写为正式工作台。

## 2. 已实现范围

### 数据库

- Flyway `V003__create_projects.sql`：项目、颜色、描述、归档时间与工作空间外键。
- Flyway `V004__create_tasks.sql`：任务状态、优先级、截止日、Today 排布、排序、乐观锁、完成和软删除时间。
- 本地开发库已执行至 `v004`；应用数据库凭据只在启动进程环境变量中提供，未写入仓库或 `application.yml`。

### 接口与规则

- 项目：分页查询、创建、详情、编辑、归档、恢复。
- 任务：分页查询、按项目查询、创建、详情、编辑、软删除、完成、重开、Today 排布和分组排序。
- Today：返回焦点、计划、稍后、已完成以及活跃项目的完成度摘要。
- 访问控制：所有项目、任务均按当前登录用户的唯一工作空间筛选；跨工作空间资源返回 `404`。
- 并发控制：任务的编辑、完成、重开、Today 排布和排序携带 `version`；陈旧请求返回 `409 TASK_VERSION_CONFLICT`。
- 归档规则：归档项目的任务可读取，但不可再新建、编辑、完成、重开或调整 Today 排布。

## 3. 验证结果

| 检查 | 结果 |
| --- | --- |
| `mvnw.cmd verify` | 通过；4 个集成测试全部通过。 |
| 测试数据库 | Docker Testcontainers 中的 MySQL 8.4；Flyway 从空库执行 `V001`–`V004`。 |
| 本地 API 健康检查 | 通过，`/actuator/health` 返回 `UP`。 |
| 真实接口冒烟 | 通过：注册 → 创建项目 → 创建焦点任务 → 查询 Today → 完成任务 → 查询已完成与项目进度。 |

## 4. 后续：M4.2 前端工作台

下一步仅改造 `flowboard-web`：接入项目/任务 API，重写桌面 Today 的任务分组、任务详情和新建任务 Dialog；随后实现项目总览、归档撤销与手机 `TaskSheet`。不实现日历、笔记、习惯、收件箱、评论、清单或工时等超出 MVP 的功能。
