# 17｜M4.2：桌面 Today 工作台实现与验收记录

## 1. 结论

桌面 Today 已完成从 M3 展示骨架到真实工作台的替换，提交并推送至 `flowboard-web` 的 `main` 分支：`3f9df5c 功能：接入 Today 项目任务工作台`。

页面只显示当前登录用户持久化在 API/MySQL 中的数据；不再使用“项目与任务能力正在接入”的假空状态或示例任务。

## 2. 已实现体验

- 左侧：今天入口、真实项目列表与新建项目入口；项目可作为 Today 筛选条件。
- 主区：日期、焦点数、新建任务、焦点/计划/稍后/已完成四个任务分组。
- 任务行：完成切换、项目色点、项目名称、截止状态、高优先级提示与选中态。
- 右侧详情：仅桌面宽度显示；包含任务状态、项目、优先级、截止日期、Today 分组、备注、编辑和删除。
- 弹窗：新建项目；新建或编辑任务（项目、状态、优先级、截止日期、Today 分组和备注）。
- 反馈：成功/错误 Toast、请求加载态、接口失败重试、按钮按下即时缩放反馈；主操作始终为 FlowBoard 蓝 `#2463EB`，没有使用黑色主按钮。

## 3. 数据边界

前端调用 M4.1 的项目、任务和 Today 接口。页面刷新后重新读取服务端数据，完成、编辑或删除后也重新同步服务端结果，避免将本地演示状态误当成真实数据。

本阶段没有加入日历、笔记、习惯、收件箱、评论、清单、工时、附件或多人协作入口。

## 4. 验证

| 检查 | 结果 |
| --- | --- |
| `npm run build` | 通过，Vue 类型检查和 Vite 生产构建均成功。 |
| 本地前端服务 | `http://127.0.0.1:5173` 返回 200。 |
| 本地 API | `/actuator/health` 返回 `UP`。 |
| 数据通路 | 由 M4.1 真实 MySQL 冒烟验证覆盖：注册 → 项目 → Today 任务 → 完成 → 已完成与项目进度。 |

## 5. 下一个阶段：M4.3

实现项目总览、项目归档后的撤销 Toast，以及手机端底部导航和可拖拽 `TaskSheet`。手机端不会把桌面右侧详情栏硬压缩进窄屏；它将使用独立的底部抽屉交互。
