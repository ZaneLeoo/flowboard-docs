# 19｜M4.2.1：任务详情并列面板与组件约束

## 1. 结论

任务详情不是独立页面。桌面端选择任务后，Today 保持在原路由和原工作流中，任务列表在左、详情在右并列显示，遵循英文布局参考的阅读与编辑节奏。

## 2. 桌面布局契约

| 条件 | 行为 |
| --- | --- |
| 视口宽度 `>= 1024px` | `TodayView` 使用两列网格：主内容自适应，详情栏固定 `352px`。 |
| 已选择任务 | 右侧渲染任务详情；左侧任务列表仍可操作，不显示遮罩。 |
| 未选择任务 | 不渲染空详情栏，主内容自然扩展。 |
| 视口宽度 `< 1024px` | 保留临时抽屉式详情适配；M4.3 再替换为可拖拽的 `TaskSheet`。 |

右侧详情栏自身只滚动详情内容，标题区与评论输入保持在同一面板内，不会创建新的路由或完整页面。

## 3. shadcn-vue 使用约束

本次通过 shadcn-vue 官方 CLI 增加以下原语，并接入实际页面：

- `Card`：Today 的任务列表与进度摘要。
- `ScrollArea`：任务详情的独立内容滚动区。
- `Separator`：任务属性与备注区之间的语义分隔。
- `Tabs`、`Popover`：Today 分组切换、任务新增入口与详情操作菜单。
- `Calendar`：任务编辑与详情的截止日期选择；项目内 `DatePicker` 只负责将 `YYYY-MM-DD` 数据契约与官方日历组合连接。

后续的选择顺序固定为：已有 FlowBoard shadcn-vue 组件 → shadcn-vue 官方 CLI 新增组件 → 官方没有对应原语时才用 Tailwind CSS 组合。

## 4. 验证

| 检查 | 结果 |
| --- | --- |
| `npm run build` | 通过；Vue 类型检查与 Vite 生产构建均成功。 |
| 路由行为 | 选择任务仅更新 Today 页面的选中状态，不发生路由跳转。 |
| 本地服务 | Vite 开发服务仍在 `http://127.0.0.1:5173` 监听，可刷新查看。 |
