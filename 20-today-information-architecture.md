# 20｜Today 职责收敛与导航页面边界

## 1. 决策

Today 是 FlowBoard 打开频率最高的执行页，只回答一个问题：**今天现在要做什么？**

因此 Today 只保留：

- 日期与“今日完成 x / y 项”轻量摘要；
- 聚焦、计划、稍后、已完成任务分组；
- 新建/编辑/完成任务；
- 选中任务后的桌面右侧详情栏，或移动端详情抽屉。

项目总览、统计仪表盘和日历规划不再作为 Today 的常驻卡片。

## 2. 当前实现结果

### 已完成

- Today 任务列表和真实 API/MySQL 数据流。
- 任务创建、编辑、删除、完成与重开。
- 桌面右侧任务详情并列面板。
- 截止日期 DatePicker、项目/优先级等详情编辑。
- Today 页面中的项目总览、每日进度环和“规划你的一天”卡片已移除。

项目数据仍会被 Today 加载，因为新建/编辑任务与右侧详情需要项目选择项；它不代表项目总览仍属于 Today。

### 尚未实现

当前左侧导航中，除 Today 外的页面仍是占位入口：

| 导航 | 当前状态 | 目标职责 |
| --- | --- | --- |
| 我的任务 | 占位 | 跨日期、跨项目的任务总览 |
| 项目 | 占位 | 项目列表、进度、任务与归档 |
| 日历 | 占位 | 按日期规划和查看任务 |
| 笔记 | 占位 | 独立笔记与任务关联 |
| 目标 / 习惯 | 占位 | 长期目标和重复行动 |
| 收集箱 / 已加星 | 占位 | 未整理任务与收藏任务 |
| 设置 | 占位 | 账户、偏好和工作区设置 |

当前路由只有 `/login`、`/register` 和 `/today`；占位入口不会伪装成已完成页面。

## 3. 后续实现顺序

1. M4.3：实现项目页，复用现有项目 API 和项目编辑能力，支持项目详情、进度、任务列表及归档撤销。
2. M4.4：实现“我的任务”页，提供跨日期和跨项目的筛选视图。
3. M4.5：实现日历页，把“规划你的一天”从 Today 迁移到按日期的工作流。
4. 后续再评估统计、笔记、目标、习惯、收集箱和已加星，不为导航数量提前堆页面。

## 4. 验证

| 检查 | 结果 |
| --- | --- |
| `npm run build` | 通过；Today 收敛后 Vue 类型检查和 Vite 构建均成功。 |
| Today 核心流程 | 保留任务分组、创建、完成、详情编辑与删除。 |
| 信息架构 | 项目、统计、日历内容不再以常驻卡片混入 Today。 |
