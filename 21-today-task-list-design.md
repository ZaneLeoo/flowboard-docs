# 21｜Today 任务列表与内联创建规范

## 1. 设计目标

Today 以“快速看清下一件事、快速记下一件事”为核心。任务列表采用参考稿的纵向分组与细分隔线，不再使用多层卡片和 Tab 让用户在不同维度之间来回切换。

## 2. 列表分组

页面展示顺序固定为：

1. **逾期**：未完成且 `dueDate < Today.date` 的任务；日期与日历图标使用危险色，右侧提供“重新安排”入口。
2. **今天**：Today 接口返回的未完成任务中，其余任务按 `todayBucket` 的 `FOCUS → PLAN → LATER` 顺序排列。
3. **已完成**：默认折叠，展示数量；展开后可查看并重新打开任务。

分组只改变展示方式，不改变现有 API 契约，也不移除 `todayBucket`。这样可以保留“聚焦 / 计划 / 稍后”的执行优先级，同时让截止日期成为用户首先感知的视觉层级。

## 3. 行级任务设计

- 左侧为圆形完成勾选框；点击任务主体打开桌面右侧详情面板，移动端打开详情抽屉。
- 主体第一行显示任务名称，第二行显示截止日期和高优先级提示（仅有数据时显示）。
- 右侧显示项目色点与项目名称；无项目时显示“收集箱”。
- 行使用轻量底部分隔线和 hover / selected 状态，不使用厚重卡片边框。
- 逾期只使用危险色提示日期，不改变 FlowBoard 的主色（`#2463EB`）。

## 4. 内联快速创建

顶部“添加任务”直接展开列表内联编辑器：

- 必填：任务名称；可选：描述、截止日期、优先级、项目。
- 任务名称使用轻量下划线输入样式，描述保持无边框文本区，避免大面积输入框打断任务流。
- 默认将任务安排到当前日期，并继承“聚焦”分组；下拉菜单可选择“聚焦 / 计划 / 稍后”。
- 底部提供收集箱 / 项目选择、取消和“添加任务”操作。
- 快速创建只负责高频捕获；任务详情面板中的“编辑任务”仍打开完整编辑对话框，承载状态、备注等低频字段。

## 5. 验收

| 检查 | 结果 |
| --- | --- |
| 逾期 / 今天 / 已完成分组 | 已实现，基于 `dueDate` 与 `today.date` 计算 |
| `todayBucket` 优先级排序 | 已保留，聚焦 → 计划 → 稍后 |
| 内联快速创建 | 已实现，复用 shadcn-vue Input、Textarea、Select、DatePicker、Button |
| 任务详情并列面板 | 保留，未改为独立页面 |
| `npm run build` | 通过 |
